CREATE TABLE CLIENTES (
	ID SERIAL PRIMARY KEY,
	NOME VARCHAR(100),
	EMAIL VARCHAR(100) UNIQUE,
	IDADE INT,
	DATA_CRIACAO TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE PEDIDOS (
	ID SERIAL PRIMARY KEY,
	CLIENTE_ID INT REFERENCES CLIENTES(ID),
	DATA_PEDIDO TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
	TOTAL NUMERIC(10, 2)
);

CREATE TABLE REGIAO (
	ID INT PRIMARY KEY,
	NOME VARCHAR(30)
);

INSERT INTO CLIENTES (NOME, EMAIL, IDADE) VALUES
('João Silva', 'joao.silva@exemplo.com', 30),
('Maria Souza', 'maria.souza@exemplo.com', 25);

SELECT * FROM CLIENTES;

INSERT INTO PEDIDOS (CLIENTE_ID, TOTAL) VALUES
(1, 150.00),
(1, 200.00),
(2, 300.00);

SELECT * FROM PEDIDOS;

INSERT INTO REGIAO VALUES
(1, 'NORTE'),
(2, 'SUL');

SELECT * FROM REGIAO;

SELECT * FROM CLIENTES;

-- ADCIONAR CONULA ENDERECO NA TABELA CLIENTES
ALTER TABLE CLIENTES ADD COLUMN ENDERECO VARCHAR(200);

SELECT * FROM CLIENTES;

-- ADD NOVO CLIENTE  NA TABELA DE CLIENTES
INSERT INTO CLIENTES (NOME, IDADE) VALUES 
('CLIENTE NOVO1', 67);

SELECT * FROM CLIENTES;

-- ADD NOVO CLIENTE  COM IDADE NULL NA TABELA DE CLIENTES
INSERT INTO CLIENTES (NOME, IDADE) VALUES 
('CLIENTE NOVO2', NULL);

SELECT * FROM CLIENTES;

-- ALTERA NOME DA COLUNA NA TABELA CLIENTES
ALTER TABLE CLIENTES RENAME COLUMN ENDERECO TO ENDERECO_RESIDENCIAL;

SELECT * FROM CLIENTES;

-- DELETAR COLUNA DA TABELA CLIENTES
ALTER TABLE CLIENTES DROP COLUMN ENDERECO_RESIDENCIAL;

SELECT * FROM CLIENTES;

-- CRIAR CHAVE ESTRANGEIRA NA TABELA CLIENTES COM A TABELA REGIAO
ALTER TABLE CLIENTES ADD COLUMN ID_REG INTEGER REFERENCES REGIAO (ID);

SELECT * FROM CLIENTES;

-- ASSOCIAR TODOS OS CLIENTES A REGIAO
UPDATE CLIENTES SET ID_REG = 1;

SELECT * FROM CLIENTES;

-- DELETAR TABELA PEDIDOS
DROP TABLE PEDIDOS;

-- DELETAR TABELA REGIAO USANDO CASCADE
DROP TABLE REGIAO CASCADE;

SELECT * FROM CLIENTES;

-- DELETAR TABELA CLIENTES
DROP TABLE CLIENTES;



