SELECT * FROM CLIENTES;

-- INSERINDO UMA LINHA NA TABELA CLIENTES
INSERT INTO CLIENTES (NOME, EMAIL, IDADE) 
VALUES ('João Siva', 'joao.silva@exemplo.com', 30);

--INSERINDO MULTIPLAS LINHA NA TABELA CLIENTES
INSERT INTO CLIENTES (NOME, EMAIL, IDADE) 
VALUES 
	('Maria Souza', 'maria.souza@exemplo.com', 25),
	('Carlos Pereira', 'carlos.pereira@exemplo.com', 40);

SELECT * FROM CLIENTES;

-- inserção com retorno de valores
INSERT INTO CLIENTES (NOME, EMAIL, IDADE)
VALUES ('Ana Lima', 'ana.lima@exemplo.com', 28)
RETURNING ID;

SELECT * FROM CLIENTES;


-- INSERÇÃO COM CONDICIONAL
INSERT INTO CLIENTES (NOME, EMAIL, IDADE)
VALUES ('João Silva', 'joao.silva@exemplo.com', 31)
ON CONFLICT (EMAIL) DO NOTHING;

SELECT * FROM CLIENTES;

-- ATUALIZAÇÃO EM CASO DE CONFLITO
INSERT INTO CLIENTES (NOME, EMAIL, IDADE)
VALUES ('João Silva', 'joao.silva@exemplo.com', 31)
ON CONFLICT (EMAIL) DO UPDATE SET IDADE = EXCLUDED.IDADE;

SELECT * FROM CLIENTES;

-- criar um backup da estrutura de uma tabela sem os dados
CREATE TABLE CLIENTES_BACKUP AS TABLE CLIENTES WITH NO DATA;

SELECT * FROM CLIENTES_BACKUP;

-- criar um backup dos dados com uma condicional
INSERT INTO CLIENTES_BACKUP (NOME, EMAIL, IDADE)
SELECT NOME, EMAIL, IDADE FROM CLIENTES WHERE IDADE > 30;

SELECT * FROM CLIENTES;

SELECT * FROM CLIENTES_BACKUP;

